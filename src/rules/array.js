/*globals describe, it*/
'use strict';
var vjs = require('../../src/main');

describe('#array', function () {
    it('should bla bla bla', function () {

        (1).should.be.eql(1);
        /*vjs.events.on('report', function (r) {
            console.log(r);
        });

        vjs.run();*/
    });
});
